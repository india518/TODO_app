<%= form_for(@team) do |f| %>

	<ul>
		<% @team.errors.full_messages.each do |error| %>
			<li><%= error %></li>
		<% end %>
	</ul>
	
	<p>
	<%= f.label :name %>
	<%= f.text_field :name %>
    </p>

	<%= f.submit %>

<% end %>
